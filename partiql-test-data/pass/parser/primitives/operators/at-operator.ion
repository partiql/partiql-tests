parse::{
    name: "at operator on identifier",
    statement: "@a",
    assert: [
        {
            result: ParseOk
        },
    ]
}

parse::{
    name: "at operator on path",
    statement: "@a.b",
    assert: [
        {
            result: ParseOk
        },
    ]
}
