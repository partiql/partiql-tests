# PartiQL Tests Validator

This package validates the schema of PartiQL tests, and is used in the *PartiQL Tests Validator* Github workflow.

## Local Validation

The following command will validate test cases locally

```shell
./gradlew clean build
```
